"""Read-only query layer over publish exports for the deep-reader agent.

v1 serves ``GET /v1/articles`` from the current publish export generation,
resolved through the ``current.json`` pointer. See ``modules/api/docs/``.
"""

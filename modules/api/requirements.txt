# API module standalone install pins (IMPLEMENTATION_PLAN.md Phase 1).
# PyYAML is pinned explicitly because it is not guaranteed to arrive as a
# transitive dependency of the web stack.
fastapi==0.141.1
uvicorn==0.51.0
PyYAML>=6,<7
